/** @type {import('./$types').ParamMatcher */
export function match(param) {
    //controllo che il nomee sia stato salvato nel database

    /*const response = await fetch('/round/check', {
        method: 'GET',
        body: JSON.stringify({
            player_game,
            player_name,
            game_round
        }),
        headers: {
            'content-type': 'application/json'
        }
    });

    return response["error"]
    */

    return true;
}